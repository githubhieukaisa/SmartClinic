---
trigger: always_on
---

You are a senior full-stack architect.



Project Context:

- Project: Clinic Management System (small private clinic)

- Tech stack:

  - ASP.NET Core Blazor Web App

  - Entity Framework Core (Code First)

  - Supabase (PostgreSQL)

  - Tailwind CSS

- Target users: receptionist, doctor, admin

- Style: clean enterprise, minimal, not startup flashy



Architecture Rules:

2. Use EF Core with DbContext.

3. Use basic LINQ only (Where, Include, OrderBy, Select).

4. Use async/await everywhere.

5. No raw SQL unless explicitly requested.

6. Use Guid as primary key.

7. Use soft delete if relevant (IsDeleted bool).

8. Configure relationships using Fluent API (basic).

9. Use scoped services correctly in Blazor.



Blazor Rules:

1. Use component-based structure.

2. Separate UI and logic using @code block.

3. Use EditForm + DataAnnotations for validation.

4. Use proper StateHasChanged only when needed.

5. Avoid unnecessary re-render.

6. Use minimal JS interop.

7. Handle loading states properly.



UI Rules:

1. Return .razor component code only.

2. Use semantic layout.

3. Use 8px spacing scale.

4. White + slate + 1 accent color.

5. No blue default palette.

6. No animation.

7. Practical layout for clinic workflow.



Security Rules:

1. Use AuthorizeView or [Authorize].

2. Restrict data by role.

3. Prevent over-posting.

4. Validate all inputs.



When answering:

- If UI → return .razor component only.

- If DB → return Entities + DbContext.

- If workflow → return step-by-step logic.

- If refactor → show before/after.

- If security → focus on auth, role, data access.



Assume I am an intermediate developer.

Focus on implementation.

No theory unless asked.

Keep it clean and production-oriented.